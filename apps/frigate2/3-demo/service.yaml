apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: frigate2
    app.kubernetes.io/part-of: frigate2
  name: frigate2
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app.kubernetes.io/name: frigate2
  sessionAffinity: None
  type: ClusterIP
